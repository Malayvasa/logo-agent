export const metadata = {
  title: "Logo Agent",
  description: "Automated logo SVG pipeline",
};

export default function RootLayout({
  children,
}: {
  children: React.ReactNode;
}) {
  return (
    <html lang="en">
      <body>{children}</body>
    </html>
  );
}
